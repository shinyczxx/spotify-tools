# Client-Only Spotify App Configuration

# Spotify API Client ID (Public - safe to expose)
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id

# Redirect URI for OAuth callback
VITE_SPOTIFY_REDIRECT_URI=http://localhost:5175/callback

# Note: This app now uses Spotify's OAuth 2.0 PKCE flow
# which is designed for client-side applications and 
# does not require a client secret.
# FRONTEND_URL=https://your-app-name.vercel.app
# NODE_ENV=production
# COOKIE_SECRET=your-production-cookie-secret-min-32-chars
